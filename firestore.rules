rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Zezwalaj tylko na odczyt/zapis dla zalogowanych użytkowników
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}